# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf

daemon=0
#no daemon
#pid=/var/run/ddclient.pid

#ssl=yes
use=web, web=checkip.dyndns.com/, web-skip='IP Address'

server={dynamic_dns_server}
login={domain}
password='{password}'
protocol={protocol}
{host}